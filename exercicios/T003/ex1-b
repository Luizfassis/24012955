#include<stdio.h>

int main(){
     int a=0;
    printf("%d\n", a);
return 0;
}
//a declaração de variável tem que ser antes do printf e ajustei a declaração para zero para não assumir outro valor que não 0
